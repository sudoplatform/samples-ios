import UIKit

/// Color constants used for mask status tags in the list view.
/// Each status has a distinct color to ensure states are visually distinguishable.
enum MaskStatusColor {
    static let enabled = UIColor.systemGreen
    static let disabled = UIColor.systemGray
    static let pending = UIColor.systemOrange
}
